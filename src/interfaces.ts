


class Project {

  create() {
    buildListOfServices()
    buildProjectUrl()
    getCurrentProjectUrlType()
  }

  getCurrentProjectUrlType(selectedType) {
    // IP:PORT/[app|ide|service_id]
    // service_id.sandbox_id.dev_id.server_host_name
  }

  getAvailableUrlScheme() {

  }
}
